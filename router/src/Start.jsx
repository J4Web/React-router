import React, { Component } from 'react';

class Start extends Component {
    render() {
        return (
            <div>
                <button>Go inside the Machine</button>
            </div>
        );
    }
}

export default Start;